# Check Collision Service - Ray-casting collision check in Octomap

# Start pose
geometry_msgs/Pose start

# Goal pose
geometry_msgs/Pose goal

---
# Is path collision-free?
bool is_safe

# Minimum distance to nearest obstacle (meters)
float32 min_distance

# Position of closest obstacle
geometry_msgs/Point closest_obstacle
